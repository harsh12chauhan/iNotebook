{
  "name": "inotebook",
  "version": "1.0.0",
  "description": "A web-based notebook seamlessly provides access to personal notes while managing user privacy through the utilization of modern technologies and libraries.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    
    "inote": "concurrently \"npm run server\" \"npm run client\"",

    "server": "cd server && nodemon index.js",  
    "client": "cd client && npm run dev -- --host"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
